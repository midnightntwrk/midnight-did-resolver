# Packages

This repository currently owns one reusable TypeScript library package:

| Package | Responsibility |
| --- | --- |
| `@midnight-ntwrk/midnight-did-secret-storage` | Local encrypted key storage, key derivation, signing, verification, and public-key helpers used by manager/service flows. |

Resolver and manager are service packages. They are documented under [Services](/services/).
