# Rust Resolver

The Rust resolver is the indexer-backed HTTP service that resolves `did:midnight` identifiers into DID Resolution Result payloads.

Use these pages when you are changing the Rust resolver binary, indexer client, serde layer, Nix build, or Docker package.

- [Development Guide](/rust/development-guide)
- [Design](/rust/design)
- [Contract Deserialization](/rust/contract-deserialization)
- [Integration Tests](/rust/integration-tests)
