export {count}

include "std";

ledger attempts: Counter;
ledger first: Boolean;
ledger successes: Counter;

circuit count(): [] {
  attempts.increment(1);
  kernel.checkpoint();
  assert !first "must be first attempt";
  first = true;
  successes.increment(1);
}
