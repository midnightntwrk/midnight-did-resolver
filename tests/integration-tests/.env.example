# Midnight DID Resolver Integration Tests - Environment Configuration

# Resolver HTTP API URL
RESOLVER_URL=http://localhost:8080

# Time to wait (in milliseconds) for the indexer to sync after blockchain operations
# Increase this if tests are failing due to "DID not found" errors
INDEXER_SYNC_DELAY=8000
