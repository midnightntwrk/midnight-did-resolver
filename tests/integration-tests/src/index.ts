import { createDID, deploy, resolve } from '@midnight-ntwrk/midnight-did-api';

console.log('Testing @midnight-ntwrk/midnight-did-api import...');
console.log('createDID:', typeof createDID);
console.log('deploy:', typeof deploy);
console.log('resolve:', typeof resolve);
console.log('Successfully imported @midnight-ntwrk/midnight-did-api!');
