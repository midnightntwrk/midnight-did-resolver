name: CI

on:
  push:
    branches: [develop, main]
  pull_request:
    branches: [develop, main]
  workflow_dispatch: {}

permissions:
  contents: read

concurrency:
  group: resolver-ci-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    name: Light strict validation
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up Node.js
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6.4.0
        with:
          node-version: "24"
          cache: npm

      - name: Install dependencies
        run: npm ci --ignore-scripts

      - name: Audit production dependencies
        run: npm audit --omit=dev --audit-level=high

      - name: Run light strict validation
        run: ./run.sh --light --strict

      - name: Build documentation
        run: ./run.sh docs

  e2e:
    name: Service E2E smoke
    runs-on: ubuntu-24.04
    timeout-minutes: 60
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up Node.js
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6.4.0
        with:
          node-version: "24"
          cache: npm

      - name: Install dependencies
        run: npm ci --ignore-scripts

      - name: Build service artifacts
        run: npm run build:all

      - name: Install Playwright browser and dependencies
        run: |
          npx playwright install-deps chromium
          npm run playwright:install -w @midnight-ntwrk/midnight-did-manager-service

      - name: Run resolver Docker integration tests
        run: npm run test:integration -w @midnight-ntwrk/midnight-did-resolver-service

      - name: Run manager standalone browser E2E tests
        run: npm run test:e2e:standalone -w @midnight-ntwrk/midnight-did-manager-service

      - name: Upload E2E diagnostics
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: service-e2e-diagnostics-${{ github.run_id }}
          if-no-files-found: warn
          retention-days: 14
          path: |
            did-manager-service/playwright-report
            did-manager-service/test-results
            did-resolver-service/test-results

  docker-runtime:
    name: Docker runtime smoke
    runs-on: ubuntu-24.04
    timeout-minutes: 60
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Build resolver image
        run: docker build --file did-resolver-service/Dockerfile --tag midnight-did-resolver:ci .

      - name: Build manager image
        run: docker build --file did-manager-service/Dockerfile --tag midnight-did-manager:ci .

      - name: Start images with demo dependencies
        env:
          DEMO_PROJECT_NAME: midnight-did-ci
          MANAGER_IMAGE: midnight-did-manager:ci
          RESOLVER_IMAGE: midnight-did-resolver:ci
          MANAGER_DATA_DIR: ${{ runner.temp }}/midnight-did-manager
        run: |
          mkdir -p "${MANAGER_DATA_DIR}"
          chmod 777 "${MANAGER_DATA_DIR}"
          ./demo/run.sh up

      - name: Verify image health endpoints
        if: always()
        env:
          DEMO_PROJECT_NAME: midnight-did-ci
          MANAGER_IMAGE: midnight-did-manager:ci
          RESOLVER_IMAGE: midnight-did-resolver:ci
          MANAGER_DATA_DIR: ${{ runner.temp }}/midnight-did-manager
        run: |
          ./demo/run.sh health
          curl --fail --retry 30 --retry-delay 2 http://127.0.0.1:3001/ready
          curl --fail --retry 30 --retry-delay 2 http://127.0.0.1:3010/ready

      - name: Print Docker runtime logs
        if: always()
        env:
          DEMO_PROJECT_NAME: midnight-did-ci
          MANAGER_IMAGE: midnight-did-manager:ci
          RESOLVER_IMAGE: midnight-did-resolver:ci
          MANAGER_DATA_DIR: ${{ runner.temp }}/midnight-did-manager
        run: docker compose -p "${DEMO_PROJECT_NAME}" -f demo/docker-compose.yml logs --no-color

      - name: Stop Docker runtime smoke
        if: always()
        env:
          DEMO_PROJECT_NAME: midnight-did-ci
          MANAGER_IMAGE: midnight-did-manager:ci
          RESOLVER_IMAGE: midnight-did-resolver:ci
          MANAGER_DATA_DIR: ${{ runner.temp }}/midnight-did-manager
        run: ./demo/run.sh down

  coverage:
    name: Coverage thresholds
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up Node.js
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6.4.0
        with:
          node-version: "24"
          cache: npm

      - name: Install dependencies
        run: npm ci --ignore-scripts

      - name: Run coverage thresholds
        run: npm run coverage

      - name: Upload coverage reports
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: coverage-reports-${{ github.run_id }}
          if-no-files-found: warn
          retention-days: 14
          path: |
            secret-storage/coverage
            did-resolver-service/coverage
            did-manager-service/coverage

  docker:
    name: Docker build (${{ matrix.application }})
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    strategy:
      fail-fast: false
      matrix:
        include:
          - application: resolver
            dockerfile: did-resolver-service/Dockerfile
          - application: manager
            dockerfile: did-manager-service/Dockerfile
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@8d2750c68a42422c14e847fe6c8ac0403b4cbd6f # v3.12.0

      - name: Validate demo Compose configuration
        run: npm run check:demo

      - name: Build image without publishing
        uses: docker/build-push-action@10e90e3645eae34f1e60eeb005ba3a3d33f178e8 # v6.19.2
        with:
          context: .
          file: ${{ matrix.dockerfile }}
          platforms: linux/amd64
          push: false
          tags: midnight-did-${{ matrix.application }}:ci
