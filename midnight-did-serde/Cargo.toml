[package]
name              = "midnight-did-serde"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
midnight-did = { workspace = true }

derive_more      = { workspace = true, features = [ "from", "display", "error" ] }
identus-apollo   = { workspace = true, features = [ "jwk" ] }
identus-did-core = { workspace = true }
paste            = { workspace = true }
serde            = { workspace = true, optional = true, features = [ "derive" ] }
serde_json       = { workspace = true }

midnight-ledger_v4 = { workspace = true }

[features]
default = [  ]
js-cli  = [ "dep:serde" ]
