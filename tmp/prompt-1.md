Investigate the repo `https://github.com/midnightntwrk/midnight-did`.
This is a private repo, so you may need to clone and investigate the sources.
If you clone, please clone to the `./tmp` directory.
Then review the file "did.compact" and the specs in the "w3c-spec/midnight-method.md" file.
With this information, list the scenario for writing the integration test for the midnight-did-resolver project.

Please focus on the resolving DID with various type of fields.
Not so much on the logic of the smart contract as that should already be covered by the "midnight-did" repository.
We want to compose the test scenarios for the resolver.
It's more important to make sure we have the cases that cover the field types with different values, and / or edge value.

